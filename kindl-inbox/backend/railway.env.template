# Copy these variable NAMES into Railway → modest-cooperation → Variables
# Paste your real values from backend/.env (never commit .env to git)
#
# Required for launch:
ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
#
# Optional (enable when you set up Stripe Pro billing):
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_PRO=
